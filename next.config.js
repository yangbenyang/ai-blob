/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'example.com',
      // 其他需要的域名...
    ],
  },
}

module.exports = nextConfig 